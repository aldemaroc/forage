# Forage environment example
# Copy to .env and adjust. Secrets NEVER go into config.yaml.
#
# Comma-separated API keys, used only when auth.enabled: true in config.yaml.
# Example: FORAGE_API_KEYS=key1,key2
FORAGE_API_KEYS=

# Container timezone
TZ=America/Recife
